# CLAUDE.md — Meteor Idle

## What this is

A 2D Unity 6 idle game with 150-level progression. Voxel meteors fall; the bottom row has 4 base slots (weapons), 2 drone bays, and 1 collector (rock grinder). Weapons auto-fire to destroy meteor voxels. Core voxel kills advance the level; CoreDrops are collected by drones and delivered to the Collector for money (upgrades only). Boss at every 10th level — slow-falling, dark/crimson, must kill to advance. Two weapon types (Missile and Railgun), collector drones with custom physics. Desktop/laptop 16:9. Early development — no persistence, no audio.

## Tech stack

- Unity **6000.4.1f1** (pinned)
- 2D URP, new Input System, TextMeshPro
- C# in `MeteorIdle` assembly (`Assets/Scripts/MeteorIdle.asmdef`)
- Tests: `Assets/Tests/EditMode/` (EditMode) and `Assets/Tests/PlayMode/` (PlayMode)
- Unity MCP (`mcpforunity://` resources, `mcp__UnityMCP__*` tools)

## Project layout

```
Assets/
  Art/                    procedurally-generated PNGs + materials
  Data/                   ScriptableObject stat assets (TurretStats, RailgunStats, DroneStats, BayStats)
  Prefabs/                BaseSlot, Meteor, Missile, RailgunRound/Streak, CollectorDrone, DroneBay, Collector, CoreDrop, UI prefabs
  Scenes/Game.unity       the one scene
  Scripts/
    LevelState.cs           level progression singleton (core-kill advancement, boss gating, difficulty scaling)
    Meteor.cs, VoxelMeteorGenerator.cs, Missile.cs
    TurretBase.cs (abstract), MissileTurret.cs, RailgunTurret.cs
    BaseSlot.cs, SlotManager.cs, MeteorSpawner.cs, GameManager.cs, SimplePool.cs, FloatingText.cs
    BuildInfo.cs          runtime accessor for Resources/BuildInfo.txt (SHA + date, gitignored)
    Drones/               Collector, CollectorDrone, CoreDrop, DroneBay, DroneBody, DroneStats, BayStats, BayManager, ThrusterTrail
    Weapons/              WeaponType, RailgunRound, RailgunStreak
    Data/                 TurretStats, RailgunStats
    Debug/DebugOverlay.cs (money setter + level picker)
    UI/                   MoneyDisplay, LevelStripUI, LevelCell, MissileUpgradePanel, RailgunUpgradePanel, DroneUpgradePanel, BuildSlotPanel, UpgradeButton, ModalClickCatcher, PanelManager, OptionsButton, OptionsPanel
  Editor/BuildScripts.cs  BuildWebGL + BuildWebGLDev entry points
Tests/
  EditMode/               ~224 tests, fast (~7s), pure logic
  PlayMode/               ~47 tests, slower (~46s), real physics/time
tools/
  identity-scrub.py       pre-commit identity-leak check
  build-webgl.sh          prod build (headless, editor must be closed)
  build-webgl-dev.sh      dev build (DEVELOPMENT_BUILD, writes .dev-build-marker)
  serve-webgl-dev.sh      python http.server :8000 on build/WebGL-dev/
  deploy-webgl.sh         rsync prod build to gh-pages worktree, does NOT push
  skills/                 project-specific reusable skills (see skills/doc-sync.md)
docs/
  superpowers/specs/      design docs per iteration
  superpowers/plans/      implementation plans per iteration
  reference/game-systems.md  detailed weapon/meteor/UI/testing/pipeline reference
```

## Conventions

**Testing:** EditMode tests for pure logic. Update/add tests when changing tested modules. Run via `mcp__UnityMCP__run_tests`. Zero failures before committing.

**Final verification gate:** Local WebGL dev build, not Unity Editor play mode. Editor play is fine for iterative debugging but not sign-off.

**Fast iterative loop (in-editor):**
1. Edit C# / `execute_code` -> `refresh_unity` -> `read_console` (zero errors)
2. `run_tests` if touching tested logic
3. `manage_editor play` -> `manage_camera screenshot` -> `read_console` -> `manage_editor stop`

**Final verification loop (WebGL dev build):**
1. `execute_code` calling `BuildScripts.BuildWebGLDev()` (editor stays open, builds in-process)
2. `tools/serve-webgl-dev.sh` in background
3. `chrome-devtools-mcp`: navigate `localhost:8000`, exercise the change, check console, screenshot
4. Close tab, kill server

**Procedural art only.** Every PNG generated by `execute_code` + `Texture2D.EncodeToPNG()`. Voxel aesthetic: per-pixel SetPixel, 1-px dark edges, ~20% brighten highlights. No smooth lines, gradients, or LineRenderers.

**Branch workflow.** Work on `iter/<name>`, never push main directly. Fast-forward main only after user verification.

**Identity rules.** Repo-local git config only — never touch global. Remote uses SSH alias `github-muwamath`. No `Co-Authored-By:` trailers. Never use `gh` CLI on this repo. Run `python3 tools/identity-scrub.py` before every commit.

**Spawner tuning.** Base intervals scale with level via `LevelState` (level 1: 15s initial/8s min, level 150: 5s/2s). Within-level ramp still applies over `rampDurationSeconds=180s`.

**Level progression.** Core voxel kills advance levels (not money). Money is purely for upgrades. Boss every 10th level (spawns alone, slow fall, dark/crimson palette). Boss failure = back 2 levels. `LevelState` singleton drives all difficulty scaling.

## Unity MCP gotchas

- `execute_code` runs C# 6 (CodeDom). No `using` inside the body — use fully-qualified types.
- `Object` is ambiguous: always write `UnityEngine.Object.DestroyImmediate(...)`.
- Don't call `MarkSceneDirty`/`SaveScene` during play mode.
- `manage_camera screenshot` excludes Screen Space Overlay canvases. Verify UI via `execute_code` CanvasGroup checks.
- `PlayerSettings.runInBackground = true` must stay on.
- **SerializeField drift.** Changing a `[SerializeField] private float x = 12f;` default in C# does NOT update existing prefabs or scene instances — their YAML wins at runtime. When "fixing" a SerializeField default, grep every `.prefab`, scene, and `.asset` file for the old value and edit the YAML too. Burned us on the Iter 4 bugfix pass: missile lifetime and boss fall speed looked fixed in code but shipped the old values for a full day.

## Debug overlay

Editor-only, backquote toggle, pauses via `Time.timeScale = 0f`. Two fields: money setter and level picker. Extend the existing `DebugOverlay.cs` — don't create new debug systems.

## Before committing / pushing

- `git status` — only expected files
- `python3 tools/identity-scrub.py` — staged diff (before commit) and `main..HEAD` (before push)
- `git log --format="%an <%ae>" -5` — only repo-local identity
- No force-push main without explicit permission
- Work on a branch, branch names: `iter/<kebab-description>`

## Before promoting a branch to `main`

1. Both test suites green (EditMode + PlayMode)
2. WebGL dev build verification (see final verification loop above)
3. Identity scrub clean
4. User sign-off
5. After fast-forward: prod WebGL build (`BuildScripts.BuildWebGL()`) -> `tools/deploy-webgl.sh` -> push gh-pages -> verify live site

## Detailed reference

For weapon stats, voxel math, UI layout, tunneling model, test coverage lists, helper patterns, and WebGL pipeline details, see [docs/reference/game-systems.md](docs/reference/game-systems.md).
